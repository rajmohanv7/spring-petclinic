pipeline {
    agent {
          label 'WIN'
          }
    stages{
        
        
      stage('Maven Build'){
            steps{
                sh 'mvn clean install'
                } 
             }
           }
       }
        
